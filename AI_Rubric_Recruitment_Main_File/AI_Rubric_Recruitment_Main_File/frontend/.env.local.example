# Server-side proxy target (optional if using NEXT_PUBLIC_BACKEND_URL everywhere)
BACKEND_URL=http://127.0.0.1:8000

# Production (Vercel): set to your Railway backend URL — browser calls API directly
# Example: NEXT_PUBLIC_BACKEND_URL=https://secure-communication-production-4f2e.up.railway.app
NEXT_PUBLIC_BACKEND_URL=
